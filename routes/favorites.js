const express = require("express");
const prisma = require("../db/db");
const router = express.Router();

// TODO: GET all favorites associated with logged in user (protected)

// TODO: POST a new favorite (protected)
